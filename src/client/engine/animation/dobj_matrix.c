#include <math.h>

/* Source: CoDUOMP.exe 0x00493820..0x00493826.
 * Unreferenced compiler-emitted float FSQRT leaf; the original source-level
 * identity is unresolved, so retain the honest binary-address name. */
float FUN_00493820(float value)
{
    return sqrtf(value);
}

/* Source: CoDUOMP.exe 0x00493830..0x00493836.
 * A second unreferenced compiler-emitted float FSQRT leaf with unresolved
 * source identity. */
float FUN_00493830(float value)
{
    return sqrtf(value);
}
